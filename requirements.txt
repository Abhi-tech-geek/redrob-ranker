# Core ranking dependencies — this is all rank.py needs.
# CPU-only, no network, no GPU. Tested on Python 3.11–3.14.
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10

# Sandbox demo app (app.py). Not needed to produce the submission CSV.
streamlit>=1.30

# OPTIONAL embedding booster (precompute_embeddings.py only). The default
# ranker does NOT require these — TF-IDF is the built-in semantic engine.
# Install with:  pip install -r requirements-embeddings.txt
#   sentence-transformers>=2.2
#   torch>=2.0
